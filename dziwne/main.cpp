#include <bits/stdc++.h>
using namespace std;

const int R = (1<<17);
bitset<1005> drzewo[R * 2];

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int n, q;
    cin >> n >> q;

    for (int i = 1; i <= n; i++){
        cin >> liczby[i];
    }

    while (q--){
        char typ;
        int a, b;
        cin >> typ >> a >> b;

    }

    return 0;
}
